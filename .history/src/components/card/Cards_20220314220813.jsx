import { useEffect, useState } from "react";
import {getData} from "../../services/api"

const Cards = () => {
    const [resu, setResponse] = useState([]);
    getData().then((param) => {
        set
    })
    
    return (
        <div>
            
        </div>
    )
}

export default Cards;